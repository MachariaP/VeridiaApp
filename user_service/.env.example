# Database Configuration
# SECURITY WARNING: Never commit real credentials to version control!
# Generate a strong password using: openssl rand -base64 32
DATABASE_URL=postgresql://veridiaapp_user:CHANGE_THIS_PASSWORD@localhost:5432/veridiapp_user_db
TEST_DATABASE_URL=postgresql://veridiaapp_user:CHANGE_THIS_PASSWORD@localhost:5432/veridiapp_user_test_db

# Docker Compose Database Configuration
POSTGRES_USER=veridiapp_user
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=veridiapp_user_db

# JWT Configuration
# IMPORTANT: Generate a strong random secret key for production
# Command: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=CHANGE_THIS_SECRET_KEY
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# API Configuration
API_V1_PREFIX=/api/v1
PROJECT_NAME=VeridiaApp User Service
